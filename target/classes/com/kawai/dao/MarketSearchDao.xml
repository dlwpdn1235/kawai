<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kawai.dao.MarketSearchDao">

	<select id="marketSearch" parameterType="Market" resultType="String">
		SELECT bookinfo.book_title , bookinfo.book_author, bookinfo.book_publisher
		FROM market
		INNER JOIN bookinfo ON market.market_id = bookinfo.bookinfo_id
		WHERE book_title = #{book_title};
	</select>

</mapper>